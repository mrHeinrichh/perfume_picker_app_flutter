# Release APK

This folder contains the Android debug APK included with the repository.

```text
perfume-picker-app-debug.apk
```

Install with:

```bash
adb install release/perfume-picker-app-debug.apk
```

This is a debug build for review and testing, not a Play Store production release.
